<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="160dp"
    android:background="@drawable/drawer_header_bg"
    android:foregroundTintMode="multiply"
    android:orientation="horizontal">

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="160dp"
        android:layout_height="match_parent"
        android:scaleType="centerInside"
        android:scaleX="1.6"
        android:scaleY="1.6"
        app:tint="?attr/colorLectureText"
        android:translationX="-100dp"
        android:translationY="36dp"
        app:srcCompat="@drawable/ic_logo_bible_mask" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:layout_marginLeft="-48dp"
        android:orientation="vertical"
        android:paddingHorizontal="16dp">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-medium"
            android:fontFeatureSettings="smcp"
            android:letterSpacing="0.0"
            android:text="La Bible"
            android:textColor="?attr/colorLectureText"
            android:textSize="34dp" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="-8dp"
            android:fontFamily="sans-serif-light"
            android:text="Traduction liturgique"
            android:textColor="?attr/colorLectureText"
            android:textSize="14dp" />

    </LinearLayout>

</LinearLayout>